packages:
  # dbt-expectations provides a suite of data quality tests. Pin to the 0.9.x
  # series so upgrades are deliberate.
  - package: metaplane/dbt_expectations
    version: [">=0.9.0", "<1.0.0"]

  # dbt-utils is a collection of community‑maintained macros and tests.
  # Pin the version to the 1.x series to match dbt-core 1.x.
  - package: dbt-labs/dbt_utils
    version: [">=1.0.0", "<2.0.0"]
